name: 公式NEWS構造診断

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest

    steps:
      - name: テスト
        run: |
          echo "公式NEWS構造診断"
          echo "GitHub Actionsは正常に動作しています"
